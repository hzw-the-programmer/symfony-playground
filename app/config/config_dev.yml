imports:
    - { resource: config.yml }

framework:
    router:
        resource: "%kernel.root_dir%/config/routing_dev.yml"
        strict_requirements: true
    profiler: { only_exceptions: false }

doctrine:
    dbal:
        path: "%kernel.root_dir%/data/blog_dev.sqlite"

web_profiler:
    toolbar: "%kernel.debug%"
    intercept_redirects: false

monolog:
    handlers:
        main:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%.log"
            level: info
        console:
            type: console
            bubble: false

#assetic:
#    use_controller: true

